# 环境变量示例
# 复制此文件为 .env.local 并填入实际值
# .env.local 不会被提交到 git

# 后端 API 地址
VITE_API_URL=http://localhost:9202

# 开发服务器端口（可选，默认 5174）
VITE_APP_PORT=5174

# Cloudflare Turnstile 公钥（必须，从 Cloudflare Dashboard 获取）
VITE_TURNSTILE_SITE_KEY=

# 可选：强制设定语言（zh / en），不配则自动检测
# VITE_LOCALE=zh
