# 前端代码适配后端字段变更总结

## 变更概述
为适配后端数据库字段从字符串枚举改为整型值的变更，前端代码已完成相应调整。

## 主要变更

### 1. 新建状态常量文件
**文件**: `src/constants/statusConstants.js`
- 定义了所有状态常量和枚举值映射
- 提供了状态文本映射和CSS类名工具函数
- 与后端枚举值保持一致

### 2. 更新服务层
**文件**: `src/services/learnService.js`
- 添加了状态常量的导入
- API调用现在传递整型状态值而非字符串

### 3. 更新管理组件
**文件**: `src/components/admin/ProfessionManagement.vue`
- 导入并使用状态常量
- 所有状态比较从字符串改为整型
- 状态选项配置使用常量定义
- 模板中的条件判断使用整型状态值

## 状态值映射

### 审批状态 (Course & Profession)
```javascript
APPROVAL_STATE = {
  SUBMITTED: 0,    // 已提交 (原 'SUBMITTED'/'SUBMITED')
  APPROVED: 1,     // 已批准 (原 'APPROVED') 
  REJECTED: 2      // 已拒绝 (原 'REJECTED')
}
```

### 进度状态 (UserCourse & UserRoadmap)
```javascript
PROGRESS_STATE = {
  NOT_STARTED: 0,  // 未开始 (原 'NOT_STARTED')
  IN_PROGRESS: 1,  // 进行中 (原 'IN_PROGRESS')
  COMPLETED: 2     // 已完成 (原 'COMPLETED')
}
```

### PostStats类型
```javascript
POST_STATS_TYPE = {
  POST: 0,         // 帖子 (原 'POST')
  ROADMAP: 1       // 学习路线 (原 'ROADMAP')
}
```

## 需要继续更新的文件

基于搜索结果，以下文件可能还需要更新：

### 高优先级 (包含状态字段)
1. `src/components/admin/CourseManagement.vue` - 课程管理组件
2. `src/views/Admin.vue` - 管理页面
3. `src/components/admin/CourseCard.vue` - 课程卡片组件

### 中优先级 (可能包含progress状态)
4. `src/views/Learning.vue` - 学习页面
5. `src/views/Course.vue` - 课程页面
6. `src/views/Self.vue` - 个人页面
7. `src/views/User.vue` - 用户页面
8. `src/views/RoadmapFlow.vue` - 路线图页面

### 低优先级 (可能只是样式相关)
9. `src/components/PostingList.vue`
10. `src/components/RightSidebar.vue`
11. `src/views/Read.vue`
12. `src/stores/postings.js`

## 下一步行动
1. 继续更新剩余的Vue组件文件
2. 测试所有状态相关功能
3. 确保API调用传递正确的整型值
4. 验证前后端数据交互正常

## 注意事项
- 前端现在完全使用整型状态值，无向后兼容
- 确保后端API已部署新版本后再更新前端
- 所有状态比较都要使用常量而非硬编码数字